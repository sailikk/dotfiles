- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.inputrc:
    ~/.tmux.conf:
    ~/.tmux:
    ~/.vim:
    ~/.vimrc:

- create:
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
